#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../statik/bin/statik" "$@"
  ret=$?
else 
  node  "$basedir/../statik/bin/statik" "$@"
  ret=$?
fi
exit $ret
